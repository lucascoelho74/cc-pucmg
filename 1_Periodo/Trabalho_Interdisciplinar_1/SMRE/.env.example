# Copie para .env e ajuste. NUNCA commite o .env.
MYSQL_ROOT_PASSWORD=troque-esta-senha
# Gere com: openssl rand -hex 32
JWT_SECRET=troque-por-uma-chave-longa-aleatoria
APP_ENV=production
CORS_ALLOW_ORIGIN=*
# false = produção sem o usuário/dados de demonstração (data.sql)
DB_SEED=true
